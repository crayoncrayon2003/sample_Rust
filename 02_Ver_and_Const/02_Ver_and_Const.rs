#[macro_use]
extern crate lazy_static;

let var1        // イミュータブルな(作成後に変更することができない)オブジェクト
let mut var2    // 変更可能な(ミュータブルな)変数
const MAX_POINTS: u32 = 100_000; //const

let x: i32 = 123;
let y: i64 = x as i64;  /// 型変換
